/** Main class of the Tetris game. */
class Main {
	/** Initializes a new game and starts it. */
	function void main() {
		var Tetris game;
		let game = Tetris.new(10, 10);
		do game.run();
		do game.dispose();
		return;
	}
}
